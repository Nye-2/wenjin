"""Single-agent workspace runtime package.

Import concrete chat, Mission loop, and contract modules directly so production
composition does not acquire a package-initialization dependency cycle.
"""
